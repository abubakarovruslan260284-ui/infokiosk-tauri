name: Build Windows kiosk (Tauri)

# Тот же принцип, что уже используется для Irskom Audio Manager:
# сборка идёт не локально, а в GitHub Actions на чистой машине с
# актуальным Rust/Node — поэтому никаких проблем с версиями
# инструментов, как в песочнице, где собирался этот прототип, здесь
# не будет: раннер сам ставит свежий toolchain.

on:
  push:
    branches: [main]
    tags: ["v*"]
  workflow_dispatch: {}

jobs:
  build-windows:
    runs-on: windows-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4

      - name: Установить Rust
        # Точная версия компилятора берётся из файла rust-toolchain.toml
        # в корне проекта (закреплена там на 1.77.2 ради Windows 7/8/8.1
        # на ваших киосках) — этот шаг просто ставит инструмент, а какую
        # именно версию использовать, rustup решает по тому файлу сам.
        uses: dtolnay/rust-toolchain@stable

      - name: Кэш Cargo
        uses: Swatinem/rust-cache@v2
        with:
          workspaces: "./src-tauri -> target"

      - name: Установить Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Установить Tauri CLI
        run: npm install -g @tauri-apps/cli

      - name: Прогнать тесты движка синхронизации (kiosk_sync)
        run: cargo test --manifest-path src-tauri/kiosk_sync/Cargo.toml

      - name: Скачать WebView2 для старых Windows (7/8/8.1)
        # Часть Windows, которая рисует интерфейс программы (WebView2),
        # Microsoft перестала обновлять для Windows 7/8/8.1 в январе
        # 2023-го. Последняя версия, которая там ещё работает, — 109.
        # Вшиваем её прямо в установщик, чтобы всё работало даже без
        # интернета на самом киоске и независимо от того, что уже
        # стоит на компьютере. (Архив последней рабочей версии — не от
        # Microsoft напрямую, а из проверенного открытого архива,
        # т.к. у самой Microsoft старые версии для скачивания уже
        # убраны со страницы.)
        shell: pwsh
        run: |
          $cabUrl = "https://github.com/westinyang/WebView2RuntimeArchive/releases/download/109.0.1518.78/Microsoft.WebView2.FixedVersionRuntime.109.0.1518.78.x64.cab"
          Invoke-WebRequest -Uri $cabUrl -OutFile "webview2-fixed.cab"
          $dest = "src-tauri/Microsoft.WebView2.FixedVersionRuntime.109.0.1518.78.x64"
          New-Item -ItemType Directory -Force -Path $dest | Out-Null
          expand webview2-fixed.cab -F:* $dest

      - name: ДИАГНОСТИКА — кто просит новые версии мелких пакетов
        # Временный шаг для расследования будущих похожих проблем.
        # Раньше эта диагностика сама падала: команда "cargo tree -i
        # <имя>" требует, чтобы в проекте была РОВНО ОДНА версия этого
        # пакета, а у нас как раз одновременно две версии одного пакета
        # (наша закреплённая старая + чья-то новая) — именно поэтому
        # команда отказывалась работать и выдавала свою же ошибку.
        # Обходим это: берём ПОЛНЫЙ граф зависимостей через "cargo
        # metadata" (он версии не уточняет) и сами в нём ищем, кто
        # требует "лишнюю" версию. Современный cargo ставим рядом
        # ВРЕМЕННО, не трогая основной закреплённый 1.77.2 — он не
        # участвует в реальной сборке. Ничего не ломает и не мешает
        # следующему шагу. Можно будет убрать, когда полностью
        # разберёмся со всеми пакетами.
        shell: pwsh
        continue-on-error: true
        run: |
          rustup toolchain install stable --profile minimal --no-self-update
          cd src-tauri
          $meta = cargo +stable metadata --format-version 1 | ConvertFrom-Json
          foreach ($targetName in @("toml_datetime", "toml_edit", "getrandom", "jobserver", "proc-macro-crate")) {
            Write-Host "===== $targetName ====="
            $targetPkgs = $meta.packages | Where-Object { $_.name -eq $targetName }
            foreach ($pkg in $targetPkgs) {
              Write-Host "--- $($pkg.name) $($pkg.version) ---"
              $dependents = $meta.resolve.nodes | Where-Object { $_.dependencies -contains $pkg.id }
              if ($dependents) {
                foreach ($dep in $dependents) {
                  Write-Host "  требуется: $($dep.id)"
                }
              } else {
                Write-Host "  (прямых зависимых узлов не найдено)"
              }
            }
          }

      - name: Принудительно понизить "мелкие" пакеты в Cargo.lock
        # Некоторые мелкие вспомогательные пакеты (toml и его семья,
        # proc-macro-crate) допускают у себя очень широкий диапазон
        # версий другого мелкого пакета — и какой-то из наших пакетов
        # (например num_enum_derive) время от времени тянет из этого
        # диапазона САМУЮ НОВУЮ версию параллельно с нашей закреплённой,
        # а новая уже требует edition2024, который старый Rust 1.77.2
        # (нужен ради Windows 7) не понимает. Обычная фиксация версии
        # строчкой в Cargo.toml здесь не помогает (несколько версий
        # одного пакета у Cargo могут стоять одновременно) — не помог
        # даже более сильный способ [patch] (у Cargo есть официально
        # известное ограничение именно для такого случая).
        # Реально работающий способ: сначала построить ПЛАН зависимостей
        # (это не сборка — исходники пакетов ещё не скачиваются и не
        # читаются, поэтому старый Cargo здесь ничего не ломает), затем
        # найти в этом плане все "нежелательные" версии из списка ниже
        # и явно понизить каждую именно до нужной нам — после этого
        # сборка их уже не встретит.
        shell: pwsh
        run: |
          $ErrorActionPreference = "Continue"
          cd src-tauri
          cargo generate-lockfile
          $desired = @{
            "toml_datetime"    = "0.7.5"
            "toml_edit"        = "0.20.2"
            "toml"             = "0.9.8"
            "toml_writer"      = "1.0.7"
            "toml_parser"      = "1.0.10"
            "serde_spanned"    = "1.0.4"
            "proc-macro-crate" = "2.0.2"
          }
          $lockText = Get-Content "Cargo.lock" -Raw
          $pkgPattern = [regex]'(?ms)^\[\[package\]\]\r?\nname = "([^"]+)"\r?\nversion = "([^"]+)"'
          foreach ($m in $pkgPattern.Matches($lockText)) {
            $pkgName = $m.Groups[1].Value
            $pkgVer = $m.Groups[2].Value
            if ($desired.ContainsKey($pkgName) -and $pkgVer -ne $desired[$pkgName]) {
              Write-Host "Понижаю $pkgName с $pkgVer до $($desired[$pkgName])"
              cargo update -p "${pkgName}:${pkgVer}" --precise $desired[$pkgName]
            }
          }

      - name: Собрать инфокиоск (.exe/.msi/.nsis)
        run: tauri build
        working-directory: .

      - name: Переименовать установщик в безопасное имя
        # Имя программы содержит кириллицу ("Инфокиоск"), и NSIS
        # собирает файл вида "Инфокиоск_1.0.3_x64-setup.exe". При
        # публикации на GitHub такое имя иногда ломает прямую ссылку
        # на скачивание (кириллица в URL). Переименовываем в
        # предсказуемое имя из латиницы — ссылка на установщик всегда
        # будет одна и та же и всегда будет рабочей.
        shell: pwsh
        run: |
          $installer = Get-ChildItem -Path "src-tauri/target/release/bundle/nsis" -Filter "*.exe" | Select-Object -First 1
          Rename-Item -Path $installer.FullName -NewName "Infokiosk-Setup-x64.exe"

      - name: Собрать «Издателя» (kiosk_publish CLI)
        run: cargo build --release --manifest-path tools/kiosk-publisher-cli/Cargo.toml

      - name: Опубликовать артефакты сборки
        uses: actions/upload-artifact@v4
        with:
          name: infokiosk-windows
          path: |
            src-tauri/target/release/bundle/nsis/*.exe
            src-tauri/target/release/infokiosk.exe
            tools/kiosk-publisher-cli/target/release/kiosk_publish.exe
          if-no-files-found: error

      - name: Опубликовать релиз со стабильной ссылкой на скачивание
        uses: softprops/action-gh-release@v2
        with:
          tag_name: build-${{ github.run_number }}
          name: "Инфокиоск — сборка №${{ github.run_number }}"
          files: |
            src-tauri/target/release/bundle/nsis/*.exe
            tools/kiosk-publisher-cli/target/release/kiosk_publish.exe
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
